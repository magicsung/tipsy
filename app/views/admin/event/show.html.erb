<h1><%= @event.title %></h1>

<table class="table table-striped">

<tr>
  <th>Description</th>
  <td><%= @event.description %></td>
</tr>

<tr>
  <th>Start_at</th>
  <td><%= @event.start_date %></td>
</tr>

<tr>
  <th>Due_at</th>
  <td><%= @event.due_date %></td>
</tr>
  
</table>

<%= link_to 'Back', admin_event_index_path, :class => "btn btn-default" %>