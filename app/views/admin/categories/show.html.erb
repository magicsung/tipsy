<h1>show</h1>

<table class="table">
  <tr>
    <td><%= @category.name %></td>
  </tr>
  <tr>
    <td><%= image_tag(@category.image.url(:medium)) %></td>
  </tr>
</table>


<%= link_to 'Back', admin_categories_path, :class => "btn btn-default" %>